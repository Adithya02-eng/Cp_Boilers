#include <bits/stdc++.h>
using namespace std;


//Just a Normal Loser Useless One//

#define fast_io ios_base::sync_with_stdio(false); cin.tie(NULL);cout.tie(NULL);

typedef long long ll;
const ll MOD = 1e9 + 7;

int gcd(int a,int b,int &x,int &y)
{
	if(b == 0)
	{
		x = 1;
		y = 0;
		return a;
	}
	int x1,y1;
	int d = gcd(a%b,b,x1,y1);
	x = y1;
	y = x1 - y1*(a/b);
	return d;
}



int main() 
{
    fast_io;
    
    int t = 1;
    //cin >> t;
    while (t--) 
    {
        
    }
    return 0;
}
